using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WetBlock : Blocks {

    //とりあえず仕様書上では普通ブロックと変わりがないため、なにも宣言していない状態。

	// Use this for initialization
	void Start () {
      //  InitMaterial();
	}
	
	// Update is called once per frame
	void Update () {
       // CheckBurnFlg();
	}
}
